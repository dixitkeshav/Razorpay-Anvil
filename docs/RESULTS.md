# Results — GENERATED, do not hand-edit

Generated by `make eval` at 2026-08-25 14:46 UTC, from seed=42, 4320 simulated minutes. Every number below is read directly from `src.evaluation.replay.replay()`'s return value — see CLAUDE.md rule #1.

## Scorecard

| Metric | Value |
|---|---|
| Incidents detected (L1) | 1 |
| Failed attempts replayed | 245 |
| Automated actions taken (RETRY + REROUTE) | 245 |
| RETRY decisions | 41 |
| REROUTE decisions | 204 |
| HOLD decisions | 0 |
| ESCALATE_HUMAN decisions | 0 |
| Recovered attempts (simulated success) | 161 |
| Ledger entries written | 245 |

## Money recovered — agent-on vs agent-off

Agent-off is the do-nothing baseline (docs/OUTCOME-MODEL.md §5): every one of these failed attempts simply stays failed. Agent-on is Anvil's policy engine acting on each one.

| | Agent-off | Agent-on |
|---|---|---|
| GMV recovered | Rs. 0.00 | Rs. 341,649.68 |

**Execution cost:** Rs. 10,200.00 (REROUTE cost, docs/OUTCOME-MODEL.md §3)

**Net incremental recovery: Rs. 331,449.68**

## How this meets Track 03 bar item 4 (measured money recovered)

The net incremental recovery figure above is the batch-level measured-money-recovered evidence: agent-on GMV recovered minus agent-off GMV recovered (always Rs. 0.00, by the do-nothing definition) minus execution cost, over every failed attempt inside every incident this run's detector found.

## Escalation and stopping-rule evidence (bar items 5 and 6)

| Escalation reason (bucketed) | Count |
|---|---|
| (none fired this run) | 0 |

| Hold reason (bucketed) | Count |
|---|---|
| (none fired this run) | 0 |

## What this evaluation cannot tell you

- **Only the currently-tuned detector's easy-tier recall is reflected here.** `h=15` (docs/JOURNAL.md Phase 3) was tuned for zero false alarms and catches the easy-tier episode reliably, but Medium/Hard-tier episodes (C, D, E, F, G) are not necessarily represented in this batch's detected-incident count — full stratified recall per tier is a Phase 13 held-out-set evaluation, not this one.
- **This is the main seed, not the held-out set.** Headline recall/precision claims belong on held-out data (Phase 13); this scorecard demonstrates the counterfactual-replay mechanism end-to-end and gives a real, reproducible rupee figure from the committed seed, not a generalization claim.
- **The outcome model's success probabilities are stated assumptions, not measured ones** — see docs/OUTCOME-MODEL.md and the Phase 13 sensitivity sweep (docs/SENSITIVITY.md) for the range this figure moves across.
- **Agent-off recovers exactly Rs. 0.00 by construction**, since the generator does not model an independent, un-assisted retry behavior (docs/OUTCOME-MODEL.md §5) — the net incremental figure above is therefore equal to agent-on's own recovery minus cost, not a comparison against a competing baseline strategy.
- **No guardrail fired in this run.** This is a real characteristic of this specific batch (one easy-tier incident, moderate amounts, confidence above threshold, and the incident's own aggregate never reached SEVERE), not a rendering gap — the zero counts above are genuine. See docs/JOURNAL.md.
