# Results — GENERATED, do not hand-edit

Generated by `make eval` at 2026-08-25 18:13 UTC, from the **held-out set** (seed=9001, 4320 simulated minutes — fresh seed, redrawn episode timing, never used to tune the detector or attribution; see docs/EPISODE-SPEC.md §7). Every number below is read directly from `src.evaluation.replay.replay()` and `src.evaluation.recall.evaluate_recall()`'s return values — see CLAUDE.md rule #1.

## Stratified recall by difficulty tier

| Tier | Episodes | Detected | Recall |
|---|---|---|---|
| easy | 1 | 1 | 100% |
| medium | 3 | 0 | 0% |
| hard | 3 | 0 | 0% |

**Decoys:** 1 of 2 decoy episodes have a slice dimension the lattice actually tests (see src.evaluation.recall.TESTABLE_DECOY_DIMS); **0 of those were falsely flagged** as incidents. This is the most informative number in this document — see 'What this evaluation cannot tell you' below.

## Scorecard (counterfactual replay)

| Metric | Value |
|---|---|
| Incidents detected (L1) | 1 |
| Failed attempts replayed | 192 |
| Automated actions taken (RETRY + REROUTE) | 192 |
| RETRY decisions | 36 |
| REROUTE decisions | 156 |
| HOLD decisions | 0 |
| ESCALATE_HUMAN decisions | 0 |
| Recovered attempts (simulated success) | 119 |
| Ledger entries written | 192 |

## Money recovered — agent-on vs agent-off

Agent-off is the do-nothing baseline (docs/OUTCOME-MODEL.md §5): every one of these failed attempts simply stays failed. Agent-on is Anvil's policy engine acting on each one.

| | Agent-off | Agent-on |
|---|---|---|
| GMV recovered | Rs. 0.00 | Rs. 276,445.87 |

**Execution cost:** Rs. 7,800.00 (REROUTE cost, docs/OUTCOME-MODEL.md §3)

**Net incremental recovery: Rs. 268,645.87**

See docs/SENSITIVITY.md for how this figure moves across the outcome model's parameter grid, including where it goes negative.

## How this meets Track 03 bar item 4 (measured money recovered)

The net incremental recovery figure above is the batch-level measured-money-recovered evidence: agent-on GMV recovered minus agent-off GMV recovered (always Rs. 0.00, by the do-nothing definition) minus execution cost, over every failed attempt inside every incident this run's detector found, on held-out data.

## Escalation and stopping-rule evidence (bar items 5 and 6)

| Escalation reason (bucketed) | Count |
|---|---|
| (none fired this run) | 0 |

| Hold reason (bucketed) | Count |
|---|---|
| (none fired this run) | 0 |

## Failure taxonomy

Read directly off the per-episode table this run scored against held-out ground truth:

| Episode | Tier | Detected |
|---|---|---|
| ep-A | easy | yes |
| ep-B | medium | no |
| ep-C | medium | no |
| ep-D | hard | no |
| ep-E | medium | no |
| ep-F | l10 | yes |
| ep-G1 | hard | no |
| ep-G2 | hard | no |
| decoy-volume-spike | decoy | n/a (untestable slice) |
| decoy-onboarding | decoy | no |

The pattern is consistent with Phase 3's own tuning finding, not a new surprise: `h=15` was chosen specifically to hold false alarms at zero (docs/JOURNAL.md Phase 3), and that same conservatism is what keeps Medium/Hard-tier episodes off this list on the held-out set too — the detector is not missing them by accident, it is declining to flag anything it cannot flag confidently. A looser threshold would recover some of these at the cost of the zero-false-alarm property Phase 3 was built around; this table is the honest cost of that trade, not a bug to silently patch.

## What this evaluation cannot tell you

- **This is one held-out draw, not a distribution.** A single fresh seed tells you this detector's recall on *this* draw of episode timing and noise, not a confidence interval over many draws — treat the recall percentages above as one honest sample, not a guaranteed rate.
- **The outcome model's success probabilities are stated assumptions, not measured ones** — see docs/OUTCOME-MODEL.md and docs/SENSITIVITY.md for the range the recovery figure moves across as those assumptions change.
- **Agent-off recovers exactly Rs. 0.00 by construction**, since the generator does not model an independent, un-assisted retry behavior (docs/OUTCOME-MODEL.md §5) — the net incremental figure above is therefore equal to agent-on's own recovery minus cost, not a comparison against a competing baseline strategy.
- **decoy-volume-spike's slice dimension (merchant category) is not one the lattice tests at any level**, so it cannot be scored as a false alarm or a correct non-detection either way — it is excluded from the decoy false-alarm count above rather than silently counted as a pass.
- **No guardrail fired in this run.** A real characteristic of this specific batch (moderate amounts, confidence above threshold, the detected incident's own aggregate never reached SEVERE), not a rendering gap — the zero counts above are genuine. See docs/JOURNAL.md.
